<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.neusoft.cas.widget"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="18" />
    <!-- 添加权限 -->
    <!-- 允许访问振动设备 -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- 请求访问使用照相设备 -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- 开启闪光灯 -->
    <uses-permission android:name="android.permission.FLASHLIGHT" />
    <!-- 允许程序访问有关GSM网络信息 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" >
    </uses-permission>
    <!-- 允许一个程序访问精良位置(如GPS) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" >
    </uses-permission>
    <!-- 允许程序打开网络套接字 -->
    <uses-permission android:name="android.permission.INTERNET" >
    </uses-permission>
    <!-- 允许程序写入外部存储器 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" >
    </uses-permission>
    <!-- 允许程序访问Wi-Fi网络状态信息 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" >
    </uses-permission>
    <!-- 允许程序改变Wi-Fi连接状态 -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" >
    </uses-permission>
    <!-- 允许只读来访问话机状态 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" >
    </uses-permission>
    <!-- 二维码扫描使用摄像头 -->
    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_logo"
        android:label="@string/app_name"
        android:theme="@style/CasTheme" >
        <activity
            android:name="com.neusoft.cas.widget.WelcomeActivity"
            android:theme="@android:style/Theme.NoTitleBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.neusoft.cas.widget.InfoFirstActivity"
            android:theme="@style/CasTheme.Sherlock" />
        <activity
            android:name="com.neusoft.cas.widget.GuideActivity"
            android:launchMode="singleInstance"
            android:theme="@android:style/Theme.NoTitleBar" />
        <activity
            android:name="com.neusoft.cas.widget.RegisterActivity"
            android:theme="@style/Theme.Sherlock.Light.NoActionBar" />
        <activity
            android:name="com.neusoft.cas.widget.LoginActivity"
            android:theme="@style/Theme.Sherlock.Light.NoActionBar" />
        <activity
            android:name="com.neusoft.cas.widget.ForgotPasswordActivity"
            android:theme="@style/Theme.Sherlock.Light.NoActionBar" />
        <activity
            android:name="com.neusoft.cas.widget.EditPersonInfoActivity"
            android:theme="@style/CasTheme.Sherlock" />
        <activity
            android:name="com.neusoft.cas.widget.InfoDetailActivity"
            android:theme="@style/CasTheme.Sherlock" />
        <activity
            android:name="com.neusoft.cas.widget.EditPasswordActivity"
            android:theme="@style/CasTheme.Sherlock" />
        <activity android:name="com.neusoft.cas.widget.FileDownloadActivity"
            android:theme="@style/CasTheme.Sherlock"/>
        <activity android:name="com.neusoft.cas.widget.ComplaintManagerActivity"
            android:theme="@style/CasTheme.Sherlock"/>
        <activity android:name="com.neusoft.cas.widget.RequirementsWorkListActivity"
            android:theme="@style/CasTheme.Sherlock"/>
         <activity android:name="com.neusoft.cas.widget.InsideWorkListActivity"
            android:theme="@style/CasTheme.Sherlock"/>
    </application>

</manifest>